# This project uses DummyJSON API - no API key required
# Base URL is hardcoded in the app

# For local development, the app runs on:
# VITE_PORT=5173

# API Base URL (already configured in the code)
# VITE_API_BASE_URL=https://dummyjson.com
